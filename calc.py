def add(x: int, y: int) -> int:
    return x + y


def sub(x: int, y: int) -> int:
    return abs(x - y)


def mul(x: int, y: int) -> int:
    return abs(x * y)


def div(x: int, y: int) -> int:
    return x / y
